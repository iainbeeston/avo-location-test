class OfficeResource < Avo::BaseResource
  self.title = :id
  self.includes = []
  # self.search_query = -> do
  #   scope.ransack(id_eq: params[:q], m: "or").result(distinct: false)
  # end

  field :id, as: :id
  field :coordinates, as: :location, stored_as: [:latitude, :longitude]
  # add fields here
end
